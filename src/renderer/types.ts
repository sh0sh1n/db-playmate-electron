export type Asset = {
  assetId: number;
  assetName: string;
  sessionId: number;
  sessionName: string;
  volumeId: string;
};

export type Participant = {
  recordId: number;
  id: string;
  gender: string;
};
